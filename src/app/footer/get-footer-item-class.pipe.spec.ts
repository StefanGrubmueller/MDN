import { GetFooterItemClassPipe } from './get-footer-item-class.pipe';

describe('GetFooterItemClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFooterItemClassPipe();
    expect(pipe).toBeTruthy();
  });
});
